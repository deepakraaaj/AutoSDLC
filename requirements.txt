fastapi==0.136.3
uvicorn==0.49.0
pydantic==2.13.4
python-multipart==0.0.32
httpx==0.28.1
python-dotenv==1.2.2
openpyxl==3.1.5
python-docx==1.2.0
litellm==1.95.0
# Not a declared litellm dependency (absent from its own Requires: list) but
# imported lazily by its retry logic whenever num_retries= is passed to
# completion() — without it, every call fails with "tenacity import failed".
tenacity==9.1.4
# LangChain/LangGraph orchestration layer (app/services/langchain_provider.py,
# app/services/langgraph_pipeline.py) — wraps the existing AIProvider/
# PhaseGenerator classes rather than replacing them; see GENERATION_ENGINE in
# main.py. Versions pinned together since langchain/langgraph/langchain-core
# ship in lockstep.
langchain==1.3.16
langchain-core==1.6.0
langgraph==1.2.11
# Repository-intelligence supplement for PR security-impact analysis
# (app/services/security/related_code.py) — structural pattern matching
# (auth/DB/HTTP/filesystem/command-execution constructs) over a repository
# snapshot. Pure static parsing (no execution of repository code); chosen
# over Joern/CodeQL/etc. per the open-source evaluation in that phase's plan
# — see related_code.py's module docstring for why the Python binding was
# used directly instead of the ast-grep CLI.
ast-grep-py==0.45.2
